package com.techiesachin.lambda.demo;

public class FunIDemo implements Runnable{

    /*The interface who contain only one abstract method but can have multiple default and
    * static method is called Functional Interface
    * Runnable-> run()
    * Callable-> call()
    * Comparable-> compareTo()
    * Comparator-> compare()*/
    @Override
    public void run() {

    }
}
